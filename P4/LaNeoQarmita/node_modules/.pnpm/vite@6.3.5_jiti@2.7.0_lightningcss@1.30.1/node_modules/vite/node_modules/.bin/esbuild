#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

"$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/jxlig0d/Escritorio/DIU/Errores404_DIU2_2526/P4/LaNeoQarmita/node_modules/.pnpm/vite@6.3.5_jiti@2.7.0_lightningcss@1.30.1/node_modules/esbuild/bin/esbuild
